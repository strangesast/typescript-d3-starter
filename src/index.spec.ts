import { expect, use } from 'chai';
import 'mocha';

describe('index', () => {
  it ('should run a test', () => {
    expect('').to.equal('');
  });
  it ('should display default body text', () => {
    // expect(document.querySelector('p').textContent).to.equal('D3 starter works');
  });
  // it ('should do this', () => {
  //   expect(this).to.be(that);
  // });
});
